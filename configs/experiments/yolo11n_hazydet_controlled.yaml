dataset:
  path: datasets/hazydet
  train_split: train
  val_split: val
  test_split: test

model_file: ../models/yolo11n.yaml
transform_file: ../transforms/basic_640.yaml

classes:
  - person
  - bicycle
  - motorcycle
  - car
  - bus
  - truck

train:
  run_key: yolo11n_hazydet_controlled
  label_mode: meta
  output_dir: runs/yolo11n/hazydet/controlled
  protocol: controlled
  max_epochs: 50
  effective_batch_size: 16
  per_device_batch_size: 8
  gradient_accumulation_steps: 2
  loader:
    num_workers: 8
    pin_memory: true
    persistent_workers: true
    prefetch_factor: 2
    predecode_images: true
  seed: 0
  eval_metric: map_50_95
  hparams:
    lr0: 0.003
    weight_decay: 0.0005
    momentum: 0.937
    warmup_epochs: 3
    cos_lr: true

eval:
  batch_size: 8
  conf_threshold: 0.001
  iou_threshold: 0.7

predict:
  batch_size: 8
  conf_threshold: 0.001
  iou_threshold: 0.7

tuning:
  study_name: yolo11n_hazydet_controlled
  n_trials: 10
  objective_metric: map_50_95
  output_dir: runs/hpo/yolo11n_hazydet_controlled
  log_to_wandb: false

search_space_file: ../search_spaces/yolo_controlled.yaml

final:
  seeds: [0, 1, 2]
  output_dir: runs/yolo11n/hazydet/controlled/final
  evaluate_val: true
