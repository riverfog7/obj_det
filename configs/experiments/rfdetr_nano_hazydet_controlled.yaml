dataset:
  path: datasets/hazydet
  train_split: train
  val_split: val
  test_split: test

model_file: ../models/rfdetr_nano.yaml
transform_file: ../transforms/basic_640.yaml

classes:
  - person
  - bicycle
  - motorcycle
  - car
  - bus
  - truck

train:
  run_key: rfdetr_nano_hazydet_controlled
  label_mode: meta
  output_dir: runs/rfdetr_nano/hazydet/controlled
  protocol: controlled
  max_epochs: 50
  batch_size: 4
  logging_steps: 10
  eval_strategy:
    enabled: false
    every_epochs: 1
  loader:
    num_workers: 4
    pin_memory: true
    persistent_workers: true
    prefetch_factor: 2
    predecode_images: false
  seed: 0
  hparams:
    learning_rate: 0.00005
    weight_decay: 0.0001
    warmup_ratio: 0.05
    lr_scheduler_type: linear
    max_grad_norm: 1.0

eval:
  batch_size: 4
  conf_threshold: 0.001
  iou_threshold: 0.7

predict:
  batch_size: 4
  conf_threshold: 0.001
  iou_threshold: 0.7

tuning:
  study_name: rfdetr_nano_hazydet_controlled
  n_trials: 10
  objective_metric: map_50_95
  output_dir: runs/hpo/rfdetr_nano_hazydet_controlled

logging:
  backends: [local, wandb]
  wandb:
    project: rfdetr_nano_hazydet_controlled

search_space_file: ../search_spaces/hf_transformer_controlled.yaml

final:
  seeds: [0, 1, 2]
  output_dir: runs/rfdetr_nano/hazydet/controlled/final
  evaluate_val: true
